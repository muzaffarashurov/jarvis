python-dotenv
PyYAML>=6.0.1
rich
loguru>=0.7.2
pyfiglet>=1.0.2
colorama>=0.4.6
openai

python-telegram-bot

SpeechRecognition
pyttsx3

# EP-046 Speech-to-Text. Vosk (offline STT engine) + sounddevice
# (microphone capture). Neither uses SpeechRecognition/pyttsx3 above --
# SpeechRecognition remains unused per EP046_DESIGN.md Section 9a
# Decision 4 (kept, not removed; not EP-046's dependency; any future
# cleanup is a separate task). pyttsx3 above is now used by EP-047
# Text-to-Speech (src/skills/voice/text_to_speech.py) -- offline,
# wraps the OS's native speech engine (SAPI5 on Windows); no model
# download, no network access.
vosk
sounddevice

# EP-048 Wake Word. openWakeWord (offline, ONNX-based wake-phrase
# detection, see src/skills/voice/wake_word.py) + the onnxruntime
# dependency it pulls in transitively. Reuses 'sounddevice' above for
# microphone access (src/skills/voice/streaming_audio_capture.py) --
# no second audio library. Pinned (mirroring PySide6's own pinned
# precedent below) since this is a model-format-sensitive dependency:
# 0.6.0 is openWakeWord's latest PyPI release as of EP-048 STEP 2
# (EP048_DESIGN.md Section 9a, owner Decision D1). Model files
# ('voice.wake.model_dir') are never downloaded automatically by this
# project -- manual installation only (owner Decision D3).
openwakeword==0.6.0

openpyxl
pandas

# EP-051 Browser Automation. Playwright is pinned to its current
# latest stable PyPI release as of EP-051 STEP 2 (EP051_DESIGN.md
# Section 8/22, Owner Decision D1). This replaces the previously-
# declared 'selenium' entry: that dependency was unpinned and unused
# (confirmed by repository inspection, EP051_DESIGN.md Section 2/6 --
# zero imports anywhere in the project), so its removal is not a
# migration away from working infrastructure. After 'pip install -r
# requirements.txt', run 'playwright install chromium' once to
# download the managed browser binary this package itself does not
# bundle -- this is a one-time setup step, not expressible purely
# through this file.
playwright==1.62.0

# EP-053 Vision Integration. Pillow decodes image files for both
# 'vision info' (metadata) and 'vision ocr' (OCR); pytesseract wraps
# the external Tesseract OCR binary for 'vision ocr' only
# (EP053_DESIGN.md Section 10, Owner Decisions D1/D2/D7). v1 is
# local-only -- no AI-provider/network dependency of any kind. After
# 'pip install -r requirements.txt', a system Tesseract OCR binary
# must also be installed and on PATH for 'vision ocr' to work (e.g.
# 'apt-get install tesseract-ocr' on Debian/Ubuntu, or the official
# installer on Windows/macOS) -- this is a one-time setup step, not
# expressible purely through this file, mirroring 'playwright install
# chromium''s own precedent above. 'vision info' has no such
# dependency and works once Pillow alone is installed (Owner Decision
# D8 -- split availability).
Pillow==12.1.1
pytesseract==0.3.13

pyautogui

psutil

requests

PySide6==6.11.2

pytest