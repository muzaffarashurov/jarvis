python-dotenv
PyYAML>=6.0.1
rich
loguru>=0.7.2
pyfiglet>=1.0.2
colorama>=0.4.6
openai

python-telegram-bot

SpeechRecognition
pyttsx3

# EP-046 Speech-to-Text. Vosk (offline STT engine) + sounddevice
# (microphone capture). Neither uses SpeechRecognition/pyttsx3 above --
# SpeechRecognition remains unused per EP046_DESIGN.md Section 9a
# Decision 4 (kept, not removed; not EP-046's dependency; any future
# cleanup is a separate task). pyttsx3 above is now used by EP-047
# Text-to-Speech (src/skills/voice/text_to_speech.py) -- offline,
# wraps the OS's native speech engine (SAPI5 on Windows); no model
# download, no network access.
vosk
sounddevice

# EP-048 Wake Word. openWakeWord (offline, ONNX-based wake-phrase
# detection, see src/skills/voice/wake_word.py) + the onnxruntime
# dependency it pulls in transitively. Reuses 'sounddevice' above for
# microphone access (src/skills/voice/streaming_audio_capture.py) --
# no second audio library. Pinned (mirroring PySide6's own pinned
# precedent below) since this is a model-format-sensitive dependency:
# 0.6.0 is openWakeWord's latest PyPI release as of EP-048 STEP 2
# (EP048_DESIGN.md Section 9a, owner Decision D1). Model files
# ('voice.wake.model_dir') are never downloaded automatically by this
# project -- manual installation only (owner Decision D3).
openwakeword==0.6.0

openpyxl
pandas

selenium

pyautogui

psutil

requests

PySide6==6.11.2

pytest