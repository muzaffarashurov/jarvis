"""EP-011 Task Scheduler package.

Contains the Scheduler's data model (job.py), its in-memory registry
(job_registry.py), and the Scheduler itself (scheduler.py). See
src/services/scheduler_service.py and src/modules/scheduler_module.py
for the service and CLI layers built on top of this package.
"""
